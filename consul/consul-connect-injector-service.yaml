apiVersion: v1
kind: Service
metadata:
  name: consul-connect-injector
  namespace: default
spec:
  ports:
  - port: 443
    targetPort: 443
  selector:
    app: consul
    component: connect-injector
