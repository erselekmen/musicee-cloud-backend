apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-consul-consul-server-1
  namespace: consul
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: efs-sc
