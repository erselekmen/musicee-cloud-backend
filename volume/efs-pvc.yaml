apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-default-consul-server-0
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ebs-sc
  resources:
    requests:
      storage: 5Gi
